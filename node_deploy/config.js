module.exports = {
    cms: {
        root: "cms_xxx",

        projectFolder: "dist",

        //压缩完zip包后是否立即删除该zip包
        justClean: false,

        //是否重启nginx
        reloadNginx: true
    },
    h5: {
        root: "h5_xxx",

        projectFolder: "dist",

        //压缩完zip包后是否立即删除该zip包
        justClean: false,

        //是否重启nginx
        reloadNginx: true
    }
};
